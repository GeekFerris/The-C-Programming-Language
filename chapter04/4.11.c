//
// Created by Ferris on 2023/6/1.
//

// #include "文件名"
// #include <文件名>

#define 名字 替换文本
/* 替换文本可以是任意的 */
/* 无限循环 */
#define forever for (;;)
/* 也可以带参数 */
#define max(A, B) ((A) > (B) ? (A) : (B))
/* 取消宏定义 */
#undef max(A, B)